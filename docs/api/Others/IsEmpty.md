# `isEmpty()`

Determines if an Enumerable is empty.

#### Returns

(*boolean*): indicate if the Enumerable is empty.

#### Example

```js
Enumerable
        .from([])
        .isEmpty();
// true
```
